- name: loops play
  hosts: local
  connection: local
  tasks:
  - name: looping task
    ansible.builtin.debug:
      msg: " Hello {{ item }} "  # item is a reserved keyword
    loop:
    - item1
    - item2
    - item3
    - item4
    - item5
    - item-N