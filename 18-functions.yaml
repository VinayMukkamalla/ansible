- name: functions
  hosts: local
  connection: local
  vars:
    name: defining variable here
  tasks:
  - name: default variable
    ansible.builtin.debug:
#      msg: "Hello {{ name }}"   --> undefined variable
       msg: "Hello {{ name | default(' providing default Value for variable name if it is not defined')}}"