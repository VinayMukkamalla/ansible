- name: Gather facts
  hosts: local
  connection: local
  # before executing any tasks on a remote server ansible will first collect information about the remote|target server and understands the server status  
  tasks:
  - name: facts
    ansible.builtin.package:
      msg: " {{ ansible_facts }} "